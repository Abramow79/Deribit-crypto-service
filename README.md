# Deribit Crypto Service

Сервис для получения котировок криптовалют (BTC, ETH) с биржи **Deribit**, их периодического сохранения в PostgreSQL и предоставления через HTTP API.

Проект реализован в рамках тестового задания на позицию **Junior Backend Developer**.

---

## 🧩 Архитектура

Проект состоит из нескольких компонентов:

- **FastAPI** — HTTP API для получения данных из базы
- **Celery** — фоновая периодическая задача для загрузки цен
- **Redis** — брокер и backend для Celery
- **PostgreSQL** — хранение данных
- **Docker / Docker Compose** — изолированный и воспроизводимый запуск

```
┌──────────┐      ┌────────┐
│ FastAPI │◄────►│ Postgres│
└────┬─────┘      └────────┘
     │
     ▼
┌──────────┐      ┌────────┐
│ Celery   │◄────►│ Redis  │
└──────────┘      └────────┘
```

## Features

- Периодический сбор цен BTC и ETH с Deribit
- Сохранение истории цен в PostgreSQL
- HTTP API для получения данных
- Фоновая обработка через Celery
- Контейнеризация с помощью Docker Compose

## Stack

- **Python 3.11**
- **FastAPI**
- **PostgreSQL**
- **Celery + Redis**
- **aiohttp**
- **SQLAlchemy**
- **pytest**
- **Docker / Docker Compose**

---

## 📁 Структура проекта

```
crypto-deribit-service/
├── app/
│   ├── api.py            # HTTP эндпоинты
│   ├── celery_app.py     # конфигурация Celery
│   ├── config.py         # настройки приложения
│   ├── database.py       # подключение к БД
│   ├── deribit_client.py # клиент Deribit API
│   ├── models.py         # SQLAlchemy модели
│   ├── tasks.py          # Celery задачи
│   └── main.py           # точка входа FastAPI
├── init_db.py            # инициализация таблиц
├── docker-compose.yml
├── Dockerfile
├── .env
└── README.md
```

---

## ⚙️ Переменные окружения (.env)

```env
# Redis
REDIS_HOST=redis
REDIS_PORT=6379

# Celery
CELERY_BROKER_URL=redis://redis:6379/0
CELERY_RESULT_BACKEND=redis://redis:6379/1

# Postgres
POSTGRES_HOST=db
POSTGRES_PORT=5432
POSTGRES_DB=crypto
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres

# App
TZ=UTC
```

---

## 🚀 Запуск проекта (Docker)

### 1️⃣ Клонировать репозиторий

```bash
git clone https://github.com/Abramow79/Deribit-crypto-service.git
cd Deribit-crypto-service
```

### 2️⃣ Запустить контейнеры

```bash
docker-compose up -d --build
```

Будут запущены:
- PostgreSQL
- Redis
- FastAPI (`http://localhost:8000`)
- Celery worker
- Celery beat

---

### 3️⃣ Инициализация базы данных

Создание таблиц выполняется отдельным контейнером:

```bash
docker-compose run --rm init_db
```

---

## 🗄 Подключение к PostgreSQL (pgAdmin)

Параметры подключения:

- **Host**: `localhost`
- **Port**: `5432`
- **Database**: `crypto`
- **Username**: `postgres`
- **Password**: `postgres`

⚠️ Если строки не обновляются в pgAdmin — нажмите **Refresh** или `F5`.

---

## 📡 HTTP API

### Получить последние цены

```
GET /prices?ticker=btc
GET /prices?ticker=eth
```

Пример:
```
http://127.0.0.1:8000/prices?ticker=btc
```

Ответ:
```json
[
  {
    "ticker": "btc",
    "price": 89986.6,
    "timestamp": 1769601481
  }
]
```

---

## ⏱ Фоновая загрузка цен

- Celery Beat запускает задачу по расписанию
- Celery Worker получает данные с Deribit API
- Цены сохраняются в таблицу `prices`

Таблица:

```sql
CREATE TABLE prices (
    id SERIAL PRIMARY KEY,
    ticker VARCHAR,
    price FLOAT,
    timestamp INTEGER
);
```
### How it works

Celery Beat по расписанию запускает задачу сбора цен

Celery Worker запрашивает цены BTC и ETH с Deribit

Полученные данные сохраняются в PostgreSQL

FastAPI предоставляет HTTP API для чтения данных

Redis используется как брокер и backend результатов Celery
---

## 🧠 Design Decisions

### Почему Docker
- Гарантирует одинаковое окружение
- Упрощает запуск без локальной установки Postgres/Redis
- Удобен для тестовых заданий и CI

### Почему Celery + Redis
- Celery — стандарт де-факто для фоновых задач в Python
- Redis лёгкий и быстрый брокер
- Хорошо масштабируется

### Почему отдельный init_db
- Явное управление миграциями
- Исключает создание таблиц при каждом старте приложения
- Удобно для тестирования

### Почему синхронный SQLAlchemy
- Проще отладка
- Нет необходимости в async DB при небольшой нагрузке

### Почему FastAPI
- Современный стандарт
- Автогенерация OpenAPI
- Высокая производительность и простота

---

## 🧪 Тестирование

(можно расширить unit-тестами через pytest)

---

## ✅ Статус проекта

- [x] Получение данных с Deribit
- [x] Сохранение в PostgreSQL
- [x] API для чтения данных
- [x] Docker Compose
- [x] Документация

---

Автор: **Андрей Абрамов**

